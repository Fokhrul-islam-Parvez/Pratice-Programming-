#include<stdio.h>
int main()
{
    long long int lli;
     long long int li;
    char a[200];
    char b[200];
    while((scanf("%s",a))!= EOF)
    {
        int l=a.length();
        for(i=0;i<l;i++)
        {
            if(a[i]==32)
            {
                lli=b.
            }
             b[i]=a[i];
        }

    }
}
